const month = `may`;

switch (month) {
    case `jan`:
        console.log(`jan`);
        break;
case `march`:
        console.log(`march`);
        break;
case `may`:
        console.log(`may`);
        break;

    default:
        break;
}